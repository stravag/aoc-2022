import org.junit.jupiter.api.Test
import kotlin.test.assertEquals

object Day12 : AbstractDay() {

    @Test
    fun part1() {
        assertEquals(0, compute(testInput))
        assertEquals(0, compute(puzzleInput))
    }

    @Test
    fun part2() {
        assertEquals(0, compute(testInput))
        assertEquals(0, compute(puzzleInput))
    }

    private fun compute(input: List<String>): Int {
        return 0
    }
}
